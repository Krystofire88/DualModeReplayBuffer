<Window x:Class="DRB.App.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Title="DualModeReplayBuffer Overlay"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown"
        Left="0"
        Top="0"
        Width="{x:Static SystemParameters.PrimaryScreenWidth}"
        Height="{x:Static SystemParameters.PrimaryScreenHeight}">
    <Grid>
        <!-- Darkened background - closes overlay when clicked outside island -->
        <Border x:Name="BackgroundOverlay"
                Background="#000000"
                Opacity="0"
                MouseLeftButtonDown="BackgroundOverlay_MouseLeftButtonDown"
                IsHitTestVisible="True"/>
    </Grid>
</Window>
